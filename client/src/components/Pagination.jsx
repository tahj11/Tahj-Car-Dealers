import React, { useState } from "react";
import { Box, Button } from "@chakra-ui/react";

const Pagination = () => {
  const [pageNumber, setPageNumber] = useState(0);

  return <Box mt="4" mb="4"></Box>;
};

export default Pagination;
